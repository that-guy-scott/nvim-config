# Neovim Configuration

My personal Neovim setup.

## Installation

```bash
git clone <repo-url> nvim-config
cd nvim-config
./install.sh
```

Then launch Neovim and plugins will install automatically.

## Files

- `init.lua` - Main configuration
- `lazy-lock.json` - Plugin version lockfile
